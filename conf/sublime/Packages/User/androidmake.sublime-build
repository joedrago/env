{
	"shell_cmd": "make configure build apk install",
	"file_regex": "^(..[^:\n]*):([0-9]+):?([0-9]+)?:? (.*)$",
	"working_dir": "${project_path:${folder:${file_path}}}",

	"variants":
	[
		{
			"name": "Clean",
			"shell_cmd": "make clean"
		}
	]
}
